var obj = $('#recordings').html("<%= escape_javascript(render 'uploads').html_safe %>");

obj.find('#uploads-table').dataTable({
  'stateSave': true,
  'columnDefs': [
    {
      'targets': 2,
      'orderable': false,
      'searchable': false
    }
  ]
});
